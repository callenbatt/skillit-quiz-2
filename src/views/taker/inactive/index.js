/**
 * TODO: display view to 'student' if 'state' is 'idle'
 */

import React from 'react';
import { FirebaseContext } from './../../../firebase';
import './style.css';

const Idle = () => {

    const { user, firebase } = React.useContext(FirebaseContext);

    return (
        this //TODO
    );
}

export default Idle;