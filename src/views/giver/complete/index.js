/**
 * TODO: display view to 'student' if 'state' is 'complete'
 */

import React from 'react';
import { FirebaseContext } from './../../../firebase';
import './style.css';

const Complete = () => {

    const { user, firebase } = React.useContext(FirebaseContext);

    return (
        this //TODO
    );
}

export default Complete;